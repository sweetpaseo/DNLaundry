{
  "name": "dn-laundry-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p public && cp -r frontend/dist/* public/"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.2",
    "hono": "^4.0.0"
  }
}
